---
title: Collapsible
description: An interactive component which expands/collapses a panel.
---

<ComponentShowcase name="collapsible-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="collapsible"
    />
  </TabsContent>
  <TabsContent value="manual">
  <Steps>
    <Step>Install Dependencies</Step>

    <InstallationCommands
      type="package-installation"
      npmCommand="npm install @base-ui/react clsx tailwind-merge"
      pnpmCommand="pnpm add @base-ui/react clsx tailwind-merge"
      yarnCommand="yarn add @base-ui/react clsx tailwind-merge"
      bunCommand="bun add @base-ui/react clsx tailwind-merge"
    />
    </Steps>

    <Step>Add cn function to your utils</Step>

    ```ts filename="lib/classes.ts"
      import { clsx, type ClassValue } from "clsx";
      import { twMerge } from "tailwind-merge";

      export function cn(...inputs: ClassValue[]) {
        return twMerge(clsx(inputs));
      }
    ```

    <Step>Copy and paste the following code into your project</Step>

    <ComponentCodePreview name="collapsible" />

  </TabsContent>
</Tabs>

## Anatomy

```tsx
import {
  Collapsible,
  CollapsiblePanel,
  CollapsibleTrigger,
} from "@/components/ui/collapsible";

<Collapsible>
  <CollapsibleTrigger>Open details</CollapsibleTrigger>
  <CollapsiblePanel>Here are the details.</CollapsiblePanel>
</Collapsible>;
```

## Animation Presets

You can use the `animationPreset` prop to change the animation preset of the collapsible.

```tsx add=2
<CollapsiblePanel
  animationPreset="scale"
>
  // your content here
</CollapsiblePanel>
```

### Scale

<ComponentShowcase name="collapsible-scale-demo" />

### Fade

<ComponentShowcase name="collapsible-fade-demo" />

### Slide Outside

<ComponentShowcase name="collapsible-slide-outside-demo" />

### Slide Inside

<ComponentShowcase name="collapsible-slide-inside-demo" />

### Motion

<ComponentShowcase name="collapsible-motion-demo" />

### Motion Blur

<ComponentShowcase name="collapsible-motion-blur-demo" />

### Perspective

<ComponentShowcase name="collapsible-perspective-demo" />

### Perspective Blur

<ComponentShowcase name="collapsible-perspective-blur-demo" />
