---
title: Kbd
description: Used to display textual user input from keyboard.
---

<ComponentShowcase name="kbd-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="kbd"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      <InstallationCommands
        type="package-installation"
        npmCommand="npm install clsx tailwind-merge"
        pnpmCommand="pnpm add clsx tailwind-merge"
        yarnCommand="yarn add clsx tailwind-merge"
        bunCommand="bun add clsx tailwind-merge"
      />

      <Step>Add cn function to your utils</Step>

      ```ts filename="lib/classes.ts"
      import { clsx, type ClassValue } from "clsx";
      import { twMerge } from "tailwind-merge";

      export function cn(...inputs: ClassValue[]) {
        return twMerge(clsx(inputs));
      }
      ```

      <Step>Copy and paste the following code into your project</Step>

      <ComponentCodePreview name="label" />
    </Steps>

  </TabsContent>
</Tabs>

## Usage

```tsx
import { Kbd } from "@/components/ui/kbd";
```

```tsx
<Kbd>F12</Kbd>
```

## Examples

### Group

<ComponentShowcase name="kbd-group-demo" />

### Button

<ComponentShowcase name="kbd-button-demo" />

### Tooltip

<ComponentShowcase name="kbd-tooltip-demo" />

### Input Group

<ComponentShowcase name="kbd-input-group-demo" />
