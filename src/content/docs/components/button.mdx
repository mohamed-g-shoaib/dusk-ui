---
title: Button
description: Used to trigger an action or event, such as submitting a form, displaying a dialog or sending a request.
---

<ComponentShowcase name="button-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="button"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Install Dependencies</Step>
      <InstallationCommands
        type="package-installation"
        npmCommand="npm install tailwind-variants clsx tailwind-merge @base-ui/react"
        pnpmCommand="pnpm add tailwind-variants clsx tailwind-merge @base-ui/react"
        yarnCommand="yarn add tailwind-variants clsx tailwind-merge @base-ui/react"
        bunCommand="bun add tailwind-variants clsx tailwind-merge @base-ui/react"
      />

      <Step>Add cn function to your utils</Step>

      ```ts filename="lib/classes.ts"
      import { clsx, type ClassValue } from "clsx";
      import { twMerge } from "tailwind-merge";

      export function cn(...inputs: ClassValue[]) {
        return twMerge(clsx(inputs));
      }
      ```

    <Step>Copy and paste the following code into your project</Step>

    <ComponentCodePreview name="button" />

  </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { Button } from "@/components/ui/button";
```

```tsx
<Button>Button</Button>
```

## Rendering as another tag

You can use the `render` prop to make another component look like a button. The button can remain keyboard accessible while being rendered as another tag, such as a `<div>`.

**NOTE**: If your rendered element is not a button, you need to pass the `nativeButton={false}` prop to the button to ensure it is still keyboard accessible. By default, the button will render a native button element.

Here's an example of a link that looks like a button.

```tsx highlight={8,9}
import Link from "next/link";

import { Button } from "@/components/ui/button";

export function LinkAsButton() {
  return (
    <Button
      render={<Link href="/login" />}
      nativeButton={false}
    >
      Login
    </Button>
  );
}
```

## Examples

### Size

<ComponentShowcase name="button-size-demo" />

### Default

<ComponentShowcase name="button-default-variant-demo" />

### Outline

<ComponentShowcase name="button-outline-variant-demo" />

### Secondary

<ComponentShowcase name="button-secondary-variant-demo" />

### Ghost

<ComponentShowcase name="button-ghost-variant-demo" />

### Destructive

<ComponentShowcase name="button-destructive-variant-demo" />

### Link

<ComponentShowcase name="button-link-variant-demo" />

### Icon

<ComponentShowcase name="button-icon-demo" />

### Icon Size

<ComponentShowcase name="button-icon-size-demo" />

### With Icon

<ComponentShowcase name="button-with-icons-demo" />

### Radius

<ComponentShowcase name="button-radius-demo" />

### Disabled

<ComponentShowcase name="button-disabled-demo" />

### Loading

<ComponentShowcase name="button-loading-demo" />

If you want focus to remain on the button when it becomes disabled, you can specify the `focusableWhenDisabled` prop. This prevents focus from being lost and maintains the tab order.

## Base UI vs Radix UI

If you’re already familiar with Radix UI and shadcn/ui, this guide highlights the small differences and similarities so you can get started with **Pure UI** quickly.

### Quick Checklist

- Replace `asChild` → `render` on `Button`

### Comparison Example

```tsx remove=2,6 add=3,4
<Button
  asChild
  render={<Link href="/login" />}
  nativeButton={false}
>
  <Link href="/login">Log in</Link>
  Log in
</Button>
```
